---
platform: linux

image_resource:
  type: registry-image
  source: {repository: zulu-openjdk:8}

inputs:
- name: concourse-demo

#caches:
#- path: gopath/

params:
#  COVERALLS_TOKEN:
  GIT_BRANCH: master

run:
  path: concourse-demo/ci/test
